<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="index.css">
    <title>Easy Share</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background-color: #f4f4f4;
        }

        .topnav {
            overflow: hidden;
            background-color: #333;
        }

        .topnav h1 {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            font-size: 24px;
            margin: 0;
        }

        .topnav a {
            float: left;
            color: #f2f2f2;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 17px;
        }

        .topnav a:hover {
            background-color: #ddd;
            color: black;
        }

        .topnav a.active {
            background-color: #04AA6D;
            color: white;
        }

        .topnav .right {
            float: right;
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        .drop-container {
            position: relative;
            display: flex;
            gap: 10px;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 200px;
            padding: 20px;
            border-radius: 10px;
            border: 2px dashed #555;
            color: #444;
            cursor: pointer;
            transition: background .2s ease-in-out, border .2s ease-in-out;
        }

        .drop-container:hover {
            background: #eee;
            border-color: #111;
        }

        .drop-container:hover .drop-title {
            color: #222;
        }

        .drop-title {
            color: #444;
            font-size: 20px;
            font-weight: bold;
            text-align: center;
            transition: color .2s ease-in-out;
        }

        .file-name {
            margin-top: 10px;
            font-size: 16px;
            color: #333;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        label {
            font-size: 18px;
            margin-bottom: 5px;
        }

        input[type="file"], textarea {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        input[type="submit"] {
            background-color: #04AA6D;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #039f5b;
        }

        h1, h2 {
            color: #333;
            text-align: center;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        ul li {
            background: #f9f9f9;
            margin: 5px 0;
            padding: 10px;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }


        footer {
            background-color: coral;
            text-align: center;
            padding: 10px;
            color: white;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
        
    </style>
</head>
<body>

    <div class="topnav">
        <h1>Easy Share</h1>
        <a class="active" href="/">Home</a>
        <a href="/converter">Converter</a>
        <a href="/contact">Contact</a>
        <a href="https://portfolio-mu-blush-10.vercel.app/">About</a> 
    </div>


<div class="container">
    <h1>File Share System</h1>
    <div class="drop-container" id="drop-container">
        <div class="drop-title">Drag & Drop Files Here</div>
        <input type="file" id="fileInput" name="file" style="display:none;">
        <div class="file-name" id="file-name"></div>
    </div>

    <form action="/upload" method="post" enctype="multipart/form-data">
        <label for="file">Upload File:</label>
        <input type="file" id="file" name="file">
        <label for="text">Enter Text:</label>
        <textarea id="text" name="text" rows="4"></textarea>
        <input type="submit" value="Upload">
    </form>

    <h1>How to Use</h1>
    <h2>File Share System</h2>
    
    <ul>
        <li>First click on choose file button and upload your file.</li>
        <li> Or you can drag and drop.</li>
        <li>Or for text, write your text in the text textbox.</li>
        <li>Then click on the submit button.</li>
        <li>After it will give you a short link.</li>
        <li>Use this link to share or download your file.</li>
    </ul>
    <p>Thank you for your time.</p>
</div>

<script>
    const dropContainer = document.getElementById('drop-container');
    const fileInput = document.getElementById('fileInput');
    const fileNameDisplay = document.getElementById('file-name');

    dropContainer.addEventListener('click', () => fileInput.click());

    dropContainer.addEventListener('dragover', (e) => {
        e.preventDefault();
        dropContainer.style.background = '#ddd';
    });

    dropContainer.addEventListener('dragleave', () => {
        dropContainer.style.background = '';
    });

    dropContainer.addEventListener('drop', (e) => {
        e.preventDefault();
        dropContainer.style.background = '';
        const files = e.dataTransfer.files;
        fileInput.files = files;
        displayFileName(files);
    });

    fileInput.addEventListener('change', (e) => {
        displayFileName(e.target.files);
    });

    function displayFileName(files) {
        if (files.length > 0) {
            fileNameDisplay.textContent = `File: ${files[0].name}`;
        } else {
            fileNameDisplay.textContent = '';
        }
    }
</script>

    <footer>
        <p {background-color: coral;} >© 2024  All rights reserved.</p>
    </footer>

    
</body>
</html>
